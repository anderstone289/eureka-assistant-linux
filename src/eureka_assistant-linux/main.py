import requests
import os
import pathlib
import dotenv

#Класс для создания объекта Стандартного мода
class StandardMode:
    COMMANDS_LIST = """
    ╔══════════════════════════════════════════════════════════════════════╗
    ║                   СПИСОК КОМАНД ВИРТУАЛЬНОГО АССИСТЕНТА              ║
    ╠══════════════════════════════════════════════════════════════════════╝
    ║
    ╟─ [ СТАНДАРТНЫЙ РЕЖИМ ] ──────────────────────────────────────────────
    ║  • "открой [программа]" — запуск приложений (браузер, блокнот и др.)
    ║  • "время" / "дата"      — текущее системное время и число
    ║  • "громкость [0-100]"   — управление уровнем звука системы
    ║  • "скриншот"            — сделать снимок экрана и сохранить в папку
    ║  • "выключить ПК"        — завершение работы операционной системы
    ║  • "путь"                — показать текущую рабочую директорию
    ║
    ╟─ [ ИНФОРМАЦИЯ И СЕТЬ ] ──────────────────────────────────────────────
    ║  • "погода [город]"      — запрос актуального прогноза через API
    ║  • "курс [валюта]"       — актуальные котировки валют и криптоактивов
    ║  • "новости"             — заголовки последних событий из мира IT
    ║  • "найти [запрос]"      — поиск информации в поисковых системах
    ║
    ╟─ [ ИНТЕЛЛЕКТУАЛЬНЫЙ РЕЖИМ (AI) ] ────────────────────────────────────
    ║  • "спроси [текст]"      — отправить свободный запрос в нейросеть
    ║  • "код [задача]"        — генерация программного кода на Python/JS
    ║  • "перевод [текст]"     — лингвистический перевод на любой язык
    ║  • "план [тема]"         — создание планов, списков и идей ИИ-моделью
    ║  • "анализ"              — проверка текста или кода на ошибки
    ║
    ╟─ [ СЕРВИСНЫЕ КОМАНДЫ ] ──────────────────────────────────────────────
    ║  • "режим"               — переключение между Standard и AI модами
    ║  • "очистить"            — очистка консоли и истории запросов
    ║  • "помощь"              — вывод этого списка команд на экран
    ║  • "выход"               — полное завершение работы ассистента
    ║
    ╚══════════════════════════════════════════════════════════════════════╝
    """
    def __init__(self):
        self.comand = input("Введите команду:")

    @staticmethod
    #Функция для выведения команд в терминал.
    def help():
        print(StandardMode.COMMANDS_LIST)
    def openfile(self):
        os.open(f"{self.comand}")
    def main(self):
        pass
#Класс для создания объекта AI мода
class AImode:
    def __init__(self):
        pass

"""Точка входа (включение программы)"""
if __name__ == '__main__':
    Standartmode = StandardMode()
